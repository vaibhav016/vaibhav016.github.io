---
title: Image Super Resolution
period: Jan 2021
order: 1
description: >-
  Converting low resolution (128x128) to high resolution (512x512) images using Generative Adversarial Networks.
  Inspired by the inability to find a high resolution video for "The Diary of Jane" by Breaking Benjamin.
  The GAN learns to generate realistic high-res images by training a discriminator to distinguish real from
  generated images, while the generator learns to produce outputs indistinguishable from real high-res images.
tech:
  - PyTorch
  - GAN
  - Computer Vision
---

The idea of using GAN was very interesting. The working of GANs can be explained by the most widely used analogy — "making the model smarter at recognising fake images from real ones". This is done by using two parallel neural networks. One will classify images into real and fake (Discriminator) and the other will generate new images.

The loss function of the discriminator is intuitive to understand. Since it's a classifier, it will be trained to distinguish between images which have labels as Real, and those which are generated by Generator (Fake). A simple binary classification task at its play!

For the Generator, we want its output to be as close to the real image, so we will make its labels as Real and then compute the loss function over the Discriminator's output probability for the synthesised images. Training these two models together for sufficiently large training steps eventually produces a generator that is capable of synthesising images which look as Real images.

Now translating this idea to our super resolution problem statement, our real images are the super resolution images, and the low resolution images when extrapolated to the high res size, become the input to the Generator. The output of a trained model would then give us high resolution images.

This idea is similar to the encoder decoder model. Both these ideas use the concept of Learning Representation from Latent Spaces.
